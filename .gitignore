.cursor/*
!.cursor/skills/

# Secrets — never commit (contains TTN_TOKEN)
.env

# Claude Code session state
.claude/

# Secret key-derivation salt (crown jewel) -- never commit
keygen_salt.h

# Firmware images handed off for flashing (rebuildable from source)
*.ino.bin

# Build artifacts from scripts/build.sh
build/

# Root-owned session junk
tmp/
